/**
 * Routing Rules Tab Component (Server)
 * 
 * Main tab component that wraps client content.
 * Server component - no interactivity, just composition.
 */

import { RoutingRulesContent } from './routing-rules-content';

export function RoutingRulesTab() {
  return <RoutingRulesContent />;
}
